import { CardsPageable.Model } from './cards-pageable.model';

describe('CardsPageable.Model', () => {
  it('should create an instance', () => {
    expect(new CardsPageable.Model()).toBeTruthy();
  });
});
